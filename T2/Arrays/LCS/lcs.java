package JAVA.T2.Arrays.LCS;
import java.util.*;

public class lcs {
    public static void main(String[] args) {
        Set<Integer> s = new HashSet<>();

        s.add(5);
        s.add(1);
        s.add(0);

        for(int i : s){
            System.out.println(i + " ");
        }
    }
}